# Deploy Report — 2026-04-12

## Контекст

- Репозиторий: `crm-front`
- Ветка: `codex/order-create-backend-ui-20260408`
- Коммит: `3e8b5d9`
- Целевой сервер: `84.47.150.248`
- Домен: `kozpavelp1.oblaka.tech`

## Что сделано

1. Поддержаны backend-апдейты для таблицы заказов в legacy-порядке колонок по документу `ORDERS_LEGACY_COLUMN_ORDER.md`.
2. Добавлены отсутствующие поля в типы:
   - `declared_volume_m3`
   - `additional_description`
3. Приведены заголовки колонок к legacy-неймингу:
   - `Клиент`, `Название фабрики`, `Дата готовности`, `Ценовой коэффициент`.
4. Локально пройдены проверки:
   - `pnpm typecheck`
   - `pnpm lint`
   - `pnpm test`
5. Изменения закоммичены и запушены в `origin/codex/order-create-backend-ui-20260408`.
6. На сервере `/opt/crm-front` выполнен `git pull` до `3e8b5d9`.
7. Выполнена раскатка фронта:
   - попытка через `docker-compose` (сборка успешна, старт сервиса уперся в конфликт уже существующего контейнера `crm-front`)
   - fallback через `docker build` + `docker run` с перезапуском контейнера `crm-front`

## Параметры рантайма контейнера

- Контейнер: `crm-front`
- Образ: `crm-front_crm-front:latest`
- Порт: `3001 -> 3000`
- Переменные:
  - `BASE_BACKEND_URL=http://84.47.150.248:8000`
  - `COOKIE_SECURE=false`
  - `NODE_ENV=production`

## Результаты smoke-проверок

Проверки после прогрева контейнера:

- `http://127.0.0.1:3001/login` → `200`
- `http://84.47.150.248:3001/login` → `200`
- `http://84.47.150.248:3001/api/auth/me` → `401` (ожидаемо без авторизации)
- `docker logs crm-front` → `Ready`

## Состояние домена

Снаружи (публичный DNS):

- `kozpavelp1.oblaka.tech` → `185.215.4.21`
- `http://kozpavelp1.oblaka.tech/login` → `404`

На самом сервере обнаружены записи в `/etc/hosts`:

- `127.0.1.1 kozpavelp1.oblaka.tech`
- `84.47.150.248 kozpavelp1.oblaka.tech kozpavelp1`

Из-за этого домен не ведет напрямую на текущий фронт на `84.47.150.248:3001` в публичном доступе.

## Итог

- Апдейты бэка поддержаны во фронте.
- Изменения запушены.
- Раскатка на сервер выполнена успешно, фронт работает по IP.
- Для полноценной доступности по домену нужна корректировка DNS/прокси-контура домена.
