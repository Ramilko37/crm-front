# Deploy Report — 2026-04-11

## Контекст

- Репозиторий: `crm-front`
- Ветка: `codex/order-create-backend-ui-20260408`
- Коммит: `ce64460`
- Целевой сервер: `84.47.150.248`
- Домен: `kozpavelp1.oblaka.tech`

## Что было сделано

1. Изменения по поддержке нового backend-контракта закоммичены и запушены.
2. На сервере обновлен код проекта в `/opt/crm-front` до коммита `ce64460`.
3. Собран production Docker-образ фронта:
   - `crm-front_crm-front:latest`
4. Выполнена раскатка фронта в контейнер:
   - контейнер: `crm-front`
   - порт: `3001 -> 3000`
   - env:
     - `BASE_BACKEND_URL=http://84.47.150.248:8000`
     - `COOKIE_SECURE=false`
     - `NODE_ENV=production`

## Технические детали раскатки

- `docker-compose` (v1.29.2) на сервере падал с ошибкой:
  - `KeyError: 'ContainerConfig'`
- Workaround:
  - удален конфликтующий старый контейнер
  - выполнен запуск контейнера `crm-front` напрямую через `docker run` из уже собранного образа

## Результаты проверок

### Сервер и контейнер

- `docker ps` показывает `crm-front` в статусе `Up`.
- `docker inspect` подтверждает, что контейнер запущен из образа `crm-front_crm-front:latest`.
- Текущий `git HEAD` на сервере: `ce64460`.

### HTTP-проверки

- `http://127.0.0.1:3001/login` → `200`
- `http://84.47.150.248:3001/login` → `200`
- `http://84.47.150.248:3001/api/auth/me` → `401` (ожидаемо без авторизации)

## Состояние домена

Проблема: домен не указывает на сервер с развернутым фронтом.

- `kozpavelp1.oblaka.tech` резолвится в `185.215.4.21`
- Фронт развернут на `84.47.150.248`

Из-за этого запросы к домену идут не в новый деплой.

## Что нужно сделать для доступа по домену

1. Изменить DNS `A`-запись `kozpavelp1.oblaka.tech` на `84.47.150.248`,
   или
2. Настроить текущий внешний прокси/DDOS-guard для проксирования на `84.47.150.248:3001`.

## Итог

- Деплой на сервер выполнен успешно.
- Приложение доступно по IP и порту.
- Блокер только на уровне DNS/внешнего прокси для доменного имени.
